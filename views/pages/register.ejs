<!DOCTYPE html>
<html lang="th">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0, viewport-fit=cover">
    <title>Labye // ลาบาย</title>
    <link rel="stylesheet" href="/css/style.css">
    <link rel="stylesheet" href="https://fonts.googleapis.com/css2?family=Prompt:wght@300;400;500;600;700&display=swap">
</head>
<body class="auth-body">

    <div class="auth-page">
        <div class="auth-brand">
            <a href="/" class="auth-logo">La bye</a>
            <p class="auth-tagline">พื้นที่สำหรับระบายความรู้สึก<br>โพสต์ได้อย่างอิสระ ไม่มีใครตัดสิน</p>
        </div>
        <div class="auth-card">
            <h1 class="auth-title">สร้างบัญชีใหม่</h1>
            <p class="auth-subtitle">ง่ายและรวดเร็ว</p>

            <% if (error) { %>
                <div class="auth-error"><%= error %></div>
            <% } %>

            <form action="/register" method="POST" class="auth-form">
                <div class="form-group">
                    <input type="text" id="username" name="username" placeholder="ชื่อผู้ใช้" required>
                </div>
                <div class="form-group">
                    <input type="email" id="email" name="email" placeholder="อีเมล" required>
                </div>
                <div class="form-group">
                    <input type="password" id="password" name="password" placeholder="รหัสผ่าน (อย่างน้อย 6 ตัวอักษร)" required minlength="6">
                </div>
                <div class="form-group">
                    <input type="password" id="confirmPassword" name="confirmPassword" placeholder="ยืนยันรหัสผ่าน" required>
                </div>
                <button type="submit" class="btn btn-success auth-btn">สมัครสมาชิก</button>
            </form>

            <div class="auth-divider"><span>หรือ</span></div>

            <p class="auth-footer">มีบัญชีอยู่แล้ว? <a href="/login">เข้าสู่ระบบ</a></p>
        </div>
    </div>

<script>
document.addEventListener('DOMContentLoaded', function() {
    document.body.classList.remove('loaded');
    setTimeout(function() {
        document.body.classList.add('loaded');
    }, 10);

    var form = document.querySelector('.auth-form');
    form.addEventListener('submit', function() {
        var btn = form.querySelector('button[type="submit"]');
        btn.disabled = true;
        btn.textContent = 'กำลังดำเนินการ...';
    });
});
</script>

<script src="/js/security.js"></script>
</body>
</html>
